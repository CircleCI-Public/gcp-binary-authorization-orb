version: 2.1

description: >
  Configure Google's Binary Authorization service to sign and certify
  container images for deployment.

display:
  source_url: https://github.com/CircleCI-Public/gcp-binary-authorization-orb

orbs:
  gcp-cli: circleci/gcp-cli@2.1.0
  # Pinned so we can use the `check-env-var-param` command.
  orb-tools: circleci/orb-tools@8.27.1
